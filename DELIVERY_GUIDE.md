# دليل تسليم وتشغيل نظام MARO ERP (On-Premise)
*للمنشآت التي لا تتوفر بها خدمة إنترنت*

## 1. مقدمة
هذا الدليل يهدف إلى ضمان تسليم واحترافية تشغيل نظام MARO ERP في موقع العميل، محاكياً كبرى الأنظمة العالمية (SAP, Odoo, Dynamics).

---

## 2. مرحلة التحضير (حقيبة العدة الذهبية)
قبل التوجه للعميل، تأكد أن "الفلاشة" الخاصة بك تحتوي على ملفات التسطيب التالية (نسخ 64-bit):

### أ. ملفات البرامج (Installers):
1. **Node.js LTS:** (محرك تشغيل النظام).
2. **PostgreSQL 16:** (قاعدة البيانات الاحترافية).
3. **مجلد النظام:** (يحتوي على مجلد `dist` وملف `package.json`).

### ب. العتاد (Hardware Check):
*   ماسح باركود (USB).
*   طابعة إيصالات حرارية.
*   جهاز حاسوب محمول (للطوارئ والتدخل التقني).

---

## 3. خطوات التسطيب (على جهاز السيرفر المحلي)

### الخطوة الأولى: التسطيب الأساسي
1. قم بتنصيب **Node.js LTS** بشكل طبيعي (Next -> Finish).
2. قم بتنصيب **PostgreSQL**.
   *   *تنبيه:* احفظ كلمة سر المستخدم (postgres).
   *   *تنبيه:* تأكد أن البورت هو `5432`.

### الخطوة الثانية: إعداد النظام
1. انقل مجلد النظام إلى جهاز العميل.
2. افتح الـ CMD أو Terminal داخل مجلد النظام.
3. قم بتشغيل الأمر: `npm install --production`.

### الخطوة الثالثة: تشغيل النظام كخدمة (الخادم الخفي)
1. قم بتنصيب أداة الـ **PM2** عالمياً: `npm install -g pm2`.
2. قم بتشغيل النظام: `pm2 start dist/server.cjs --name "MARO_ERP"`.
3. لضمان عمله تلقائياً عند تشغيل الجهاز، استخدم: `pm2 save` ثم `pm2 startup`.

---

## 4. إعدادات جهاز العميل (لتحويل الرابط إلى برنامج)
بما أن النظام يعمل الآن على الشبكة الداخلية (مثلاً `http://192.168.1.100:3000`):

1. افتح متصفح Chrome على جهاز الكاشير (أو أي جهاز بالشبكة).
2. اكتب عنوان السيرفر.
3. من القائمة (الثلاث نقاط) -> **Save and share** -> **Install page as app**.
4. سيظهر أيقونة البرنامج على سطح المكتب.

---

## 5. قائمة تسليم العميل (Handover Checklist)

| المادة | الحالة | ملاحظات |
| :--- | :--- | :--- |
| **ورقة بيانات الدخول** | [ ] | (رابط السيرفر، اسم المستخدم، كلمة السر) |
| **دليل المستخدم (GUIDE)** | [ ] | تسليم دليل المستخدم (مطبوع أو PDF) |
| **التدريب السريع** | [ ] | شرح (البيع، الطباعة، التقارير) |
| **خطة النسخ الاحتياطي** | [ ] | تعليم العميل سحب Backup يومي على فلاشة |

---
*تم إعداد هذا الدليل لضمان تسليم احترافي لنظام MARO ERP.*
