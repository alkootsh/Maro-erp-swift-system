# توثيق ميزات نظام سويفت ERP

هذا المستند يلخص الميزات والوحدات الرئيسية المتاحة في تطبيق سويفت ERP.

## الوحدات الرئيسية (Modules)

1. **لوحة التحكم (Dashboard):**
   - عرض ملخص الأداء العام للمبيعات، المشتريات، والمخزون.

2. **إدارة المخزون (Inventory & Products):**
   - إدارة المنتجات (إضافة، تعديل، حذف).
   - إدارة المخازن (Warehouses).
   - تتبع مستويات المخزون (الحد الأدنى، الكميات).

3. **المبيعات (Sales & POS):**
   - نقطة بيع (POS) لإتمام عمليات البيع بسرعة.
   - إدارة الفواتير (Invoices) وتتبع المدفوعات.
   - إدارة مرتجعات المبيعات (Returns).

4. **المشتريات (Purchases & Suppliers):**
   - إدارة فواتير المشتريات (Bills).
   - إدارة الموردين (Suppliers).

5. **العملاء والحسابات (Customers & Transactions):**
   - إدارة العملاء (Customers).
   - تتبع المعاملات المالية (Transactions).

6. **التقارير والإعدادات (Reports & Settings):**
   - وحدة التقارير (Reports) لتحليل البيانات.
   - إعدادات النظام (Settings) وإعدادات الفواتير (Invoice Settings) والتنبيهات (Alert Settings).

7. **المساعد الذكي (AI Assistant):**
   - مساعد ذكي يعتمد على Gemini لتحليل بيانات Firebase مباشرة وتقديم تقارير واستشارات محاسبية.

## المكونات التقنية
- **واجهة المستخدم:** React مع CustomTailwind (مضمن في المشروع).
- **قاعدة البيانات:** Firebase Firestore.
- **التوثيق:** Firebase Auth.
- **الذكاء الاصطناعي:** تكامل مع Gemini API (عبر خادم Express).
